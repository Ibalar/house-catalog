# Быстрый старт

Краткая инструкция для быстрого развёртывания проекта.

## Предварительные требования

- PHP >= 8.3
- Composer
- MySQL >= 5.7
- Node.js и NPM (опционально)

## Шаги установки

### 1. Установка зависимостей

```bash
composer install
```

### 2. Настройка окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=your_password
```

### 3. Генерация ключа приложения

```bash
php artisan key:generate
```

### 4. Создание базы данных

В MySQL:

```sql
CREATE DATABASE laravel CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 5. Миграции

```bash
php artisan migrate
```

### 6. Создание администратора

```bash
php artisan moonshine:user --username=admin@example.com --name=Admin --password=password
```

⚠️ **Важно**: Измените пароль на более надёжный!

### 7. Запуск приложения

```bash
php artisan serve
```

### 8. Доступ

- **Сайт**: http://localhost:8000
- **Админ-панель**: http://localhost:8000/admin
- **Логин**: admin@example.com
- **Пароль**: password (или тот, что вы указали)

## Готово! 🎉

Теперь вы можете:
- Войти в админ-панель по адресу `/admin`
- Создавать новые ресурсы: `php artisan moonshine:resource ProductResource`
- Добавлять страницы: `php artisan moonshine:page CustomPage`

## Полная документация

Смотрите `README.md` для подробной документации.
